using Xunit;

namespace Solitaire.Engine.Tests;

/// <summary>
/// Placeholder so the test harness has something to run until real engine
/// tests (and shared test-vector replay) are added. Safe to delete later.
/// </summary>
public class SkeletonSmokeTests
{
    [Fact]
    public void Harness_Runs()
    {
        Assert.True(true);
    }
}
